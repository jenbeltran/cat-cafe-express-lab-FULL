let express;_ba7‍.w("express",[["default",["express"],function(v){express=v}]]);
const app = express();

// allows us to parse json
app.use(express.json());
app.use(router);

app.listen(3000, () => console.log("Express server is running"));
